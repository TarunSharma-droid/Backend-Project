//               NOTES

// New-Item app.js, constants.js, index.js --> shortcut to create multiple file in windows using terminal

// touch app.js, constants.js, index.js --> shortcut to create multiple file in windows using terminal

// "type": "module", --> we can also make it "commonjs"

// package.json  -->	    Lists the packages your project uses
// package-lock.json --->   Stores the exact installed versions

// Nodemon is online available utility which is used to restart the server as soon as we save the file.
// it is installed in the project using --> npm i -D nodemon
